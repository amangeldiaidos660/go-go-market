# Go-Go Market

Кроссплатформенное приложение для маркетплейса, разработанное с использованием React Native, Expo и NativeWind (Tailwind CSS).

## 🚀 Технологический стек

- **React Native** - фреймворк для разработки мобильных приложений
- **Expo** - платформа для упрощения разработки и сборки
- **NativeWind** - Tailwind CSS для React Native
- **react-native-web** - поддержка веб-платформы

## 📦 Структура проекта

```
gogomarket/
├── src/
│   ├── components/     # Переиспользуемые компоненты
│   ├── screens/        # Экраны приложения
│   └── assets/         # Изображения, шрифты и другие ресурсы
├── App.js              # Главный компонент приложения
├── global.css          # Глобальные стили Tailwind
├── tailwind.config.js  # Конфигурация Tailwind
├── babel.config.js     # Конфигурация Babel
├── metro.config.js     # Конфигурация Metro bundler
└── app.json            # Конфигурация Expo
```

## 🛠 Первый запуск

### Предварительные требования

Убедитесь, что у вас установлены:
- **Node.js** (v16 или выше) - [Скачать](https://nodejs.org/)
- **npm** или **yarn**

### Установка зависимостей

```bash
cd d:\Workspace\gogomarket
npm install
```

### Запуск проекта

#### 🌐 Веб-версия (для локального тестирования)

```bash
npm run web
```

Приложение откроется в браузере по адресу: `http://localhost:8081`

#### 📱 Android (для тестирования на устройстве)

**Вариант 1: Использование Expo Go (быстрый способ)**
1. Установите приложение [Expo Go](https://expo.dev/client) на ваше Android устройство
2. Запустите команду:
   ```bash
   npm start
   ```
3. Отсканируйте QR-код через Expo Go

**Вариант 2: Сборка APK файла**
```bash
npm install -g eas-cli
eas login
eas build --platform android --profile preview
```

#### 🍎 iOS (требуется macOS)

**Вариант 1: Использование Expo Go**
1. Установите [Expo Go](https://expo.dev/client) на ваше iOS устройство
2. Запустите:
   ```bash
   npm start
   ```
3. Отсканируйте QR-код через Expo Go

**Вариант 2: Локальная сборка**
```bash
npm run ios
```

## 📋 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm start` | Запуск Expo Dev Server |
| `npm run web` | Запуск веб-версии |
| `npm run android` | Запуск на Android |
| `npm run ios` | Запуск на iOS (только macOS) |

## 🎨 Использование Tailwind CSS

В проекте настроен NativeWind, который позволяет использовать Tailwind классы напрямую:

```jsx
import { View, Text } from 'react-native';

export default function Component() {
  return (
    <View className="flex-1 bg-blue-500 items-center justify-center">
      <Text className="text-white text-2xl font-bold">
        Привет, Go-Go Market!
      </Text>
    </View>
  );
}
```

## 🌍 Деплой веб-версии

### Экспорт статического сайта

```bash
npx expo export -p web
```

Готовые файлы будут в папке `dist/`. Их можно загрузить на любой хостинг:
- Vercel
- Netlify
- GitHub Pages
- Любой статический хостинг

### Пример деплоя на Vercel

```bash
npm install -g vercel
npx expo export -p web
vercel --prod
```

## 🔧 Настройка для production

### Android APK

1. Зарегистрируйтесь на [Expo](https://expo.dev)
2. Установите EAS CLI:
   ```bash
   npm install -g eas-cli
   eas login
   ```
3. Настройте проект:
   ```bash
   eas build:configure
   ```
4. Создайте production сборку:
   ```bash
   eas build --platform android --profile production
   ```

### iOS App

```bash
eas build --platform ios --profile production
```

## 📝 Дальнейшая разработка

Начните разработку с редактирования файла [App.js](App.js).

Создавайте новые компоненты в папке `src/components/` и экраны в `src/screens/`.

## 🐛 Решение проблем

### Ошибка при запуске Metro
```bash
npx expo start -c
```

### Проблемы с кешем
```bash
npm start -- --clear
```

### Переустановка зависимостей
```bash
rm -rf node_modules package-lock.json
npm install
```

## 📚 Полезные ссылки

- [Документация Expo](https://docs.expo.dev/)
- [React Native](https://reactnative.dev/)
- [NativeWind](https://www.nativewind.dev/)
- [Tailwind CSS](https://tailwindcss.com/)

---

**Версия:** 1.0.0  
**Лицензия:** Private  
**Автор:** Go-Go Market Team
